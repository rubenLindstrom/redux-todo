export default (todoApp = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
});

const initialState = {
  visibilityFilter: VisibilityFilters.SHOW_ALL,
  todos: []
};

export default (todoApp = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
});
